# 默认使用淘宝 npm 镜像（npmmirror），加速国内 npm ci / npm install。
# 若需使用官方源：删除本文件，或改为 registry=https://registry.npmjs.org/
# 单次覆盖示例：npm ci --registry=https://registry.npmjs.org/
registry=https://registry.npmmirror.com/
